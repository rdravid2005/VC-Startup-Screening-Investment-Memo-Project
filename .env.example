# Optional. The app remains fully usable with a deterministic local memo when blank.
OPENAI_API_KEY=

# Optional model override. The default is a cost-conscious model suitable for demos.
OPENAI_MODEL=gpt-4.1-mini
